const Hint = ({ hint }) => {
    return (
      <p className="mt-4 text-sm italic text-black">
        Hint: <span className="text-black">{hint}</span>
      </p>
    );
  };
  
  export default Hint;
  